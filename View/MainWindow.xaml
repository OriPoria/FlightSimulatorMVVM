<Window x:Class="flightSimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        xmlns:Controls ="clr-namespace:flightSimulator.controls"
        xmlns:u="clr-namespace:flightSimulator.View.controls"
                xmlns:v="clr-namespace:flightSimulator.View.controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" Closed="Window_Closed">
    <Viewbox>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="400"/>
                <RowDefinition Height="200"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>



            <v:DashBoard x:Name="dash" Grid.Column="1" Grid.Row="0"/>

            <v:Steering Grid.Column="1" Grid.Row="1"/>

            <u:Map x:Name="myMapView" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"/>


        </Grid>
    </Viewbox>

</Window>